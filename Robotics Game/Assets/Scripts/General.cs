using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class General : MonoBehaviour
{
    void OnGUI() {
        //GUI.Label(new Rect(10, 10, 100, 20), "" + Mathf.Floor(Time.time));
    }
}
